import React, { ReactElement } from 'react';

export default function StartPage(): ReactElement {
  return (
    <div className="bg-black w-full h-full flex justify-center items-center">
      <h1 className="text-xl text-white">Randomizer Note Clicker</h1>
    </div>
  );
}
